'use strict'
import React from 'react'
import ReactDOM from 'react-dom'

if (module.hot) {
  module.hot.accept()
}

const Card = (props) => {
  return (
    <div>
      <img />
    </div>
  )
}

ReactDOM.render(<App />, mountNode)
